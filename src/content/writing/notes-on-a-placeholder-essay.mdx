export const meta = {
  title: "Notes on a placeholder essay",
  date: "2026-04-10",
  excerpt: "A first sentence that previews what the post is about.",
};

This is a placeholder essay. Replace this body with the actual writing.

## A subheading

You can use **bold**, *italic*, [links](https://example.com), and lists:

- bullet one
- bullet two
- bullet three

> Blockquotes work too. Useful for pulling out a sentence.

```js
// And code blocks render as monospace.
function hello(name) {
  return `Hello, ${name}`;
}
```

Closing thought goes here.
